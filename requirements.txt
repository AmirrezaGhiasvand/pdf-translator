# PyTorch - install manually with CUDA support (after installing requirements):
# pip install torch --index-url https://download.pytorch.org/whl/cu124
# Requires Python 3.11, not compatible with 3.14+

# PDF processing
pypdf

# Core ML
transformers
datasets==3.6.0
sentencepiece
sacremoses
langchain
langchain-huggingface
langchain-community

# Pinned for stability on Windows + Python 3.11
pyarrow==21.0.0
pandas==2.1.4

# Fine-tuning
accelerate
peft
trl
bitsandbytes

# Evaluation
sacrebleu

# Utilities
tqdm